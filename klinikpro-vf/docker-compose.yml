services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: klinikpro
      POSTGRES_USER: klinik
      POSTGRES_PASSWORD: klinik_dev
    ports: ["5432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
volumes:
  pgdata:
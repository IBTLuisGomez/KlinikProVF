server:
    port: 8081
spring:
    datasource:
        url: jdbc:postgresql://localhost:5433/klinikpro # (o 5432 según lo que quedó)
        username: klinik
        password: klinik_dev
    jpa:
        hibernate:
            ddl-auto: validate
        open-in-view: false
management:
    endpoints:
        web:
            exposure:
                include: health,info
app:
    version: VF1-0.0.1
